# teal

A transpiler from [Teal](https://teal-language.org/) to Lua without type checking. Similarly to how NodeJS can execute TypeScript code directly, this helps remove a build step when using typing.
